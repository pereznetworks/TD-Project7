extends layout.pug

block content
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9

        h2= `${status}`

        p.errorNoPadding.lead.text-light= `${msg}`
        p.errorSmallFont.lead.text-light= `${error.stack}`
